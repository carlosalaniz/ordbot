import { execSync } from 'child_process'

export default {
    async inscribe(wallet_name: string, file_path: String, fee_rate: number, destination_wallet: string) {
        // I don't know how to do this yet.
        execSync("ord")
    }
}